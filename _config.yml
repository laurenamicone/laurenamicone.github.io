domain: laurenamicone.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://laurenamicone.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: /laurenamicone.github.io/      # place folder name if the site is served in a subfolder
theme: jekyll-theme-minimal
